<Window x:Class="ShoeAccounting.Views.Windows.ProductManagment"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShoeAccounting.Views.Windows"
        xmlns:converters="clr-namespace:ShoeAccounting.Utils"
        mc:Ignorable="d"
        Title="Управление товаром" Height="900" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <converters:ManagmentSettingsToTitle x:Key="ManagmentSettingsToTitle"/>
        <converters:BlobToImageConverter x:Key="BlobToImageConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="3"
                   Text="{Binding IsProductNew, Converter={StaticResource ManagmentSettingsToTitle}}"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <Border Grid.Row="2" Grid.Column="0"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                CornerRadius="5"
                Padding="10"
                Background="#F9F9F9">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="Изображение товара"
                           FontSize="14"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,5"/>

                <Border BorderBrush="#AAAAAA" 
                        BorderThickness="1" 
                        CornerRadius="3"
                        Padding="2">
                    <Image x:Name="productImage"
                           Source="{Binding CurrentProduct.ProductPhoto, Converter={StaticResource BlobToImageConverter}}"
                           Width="250" 
                           Height="180"
                           Stretch="Uniform"/>
                </Border>

                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center"
                            Margin="0,10,0,0">
                    <Button x:Name="loadProductImageButton" 
                            Content="Загрузить"
                            Width="80"
                            Height="25"
                            Margin="5,0"
                            FontSize="12"
                            Click="loadProductImageButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="4" Grid.Column="0">
            <TextBlock Text="Наименование товара *"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,0,0,3"/>
            <TextBox x:Name="productTitleTextBox"
                     Text="{Binding CurrentProduct.ProductTitle, UpdateSourceTrigger=PropertyChanged}"
                     Height="28"
                     FontSize="14"
                     Padding="5"/>

            <TextBlock Text="Артикул *"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,15,0,3"/>
            <TextBox x:Name="productArticleTextBox"
                     Text="{Binding CurrentProduct.ProductArticle, UpdateSourceTrigger=PropertyChanged}"
                     Height="28"
                     FontSize="14"
                     Padding="5"
                     MaxLength="6"/>

            <TextBlock Text="Категория"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,15,0,3"/>
            <ComboBox x:Name="productCategoryComboBox"
                      ItemsSource="{Binding Categories}"
                      DisplayMemberPath="CategoryTitle"
                      SelectedValuePath="CategoryId"
                      SelectedValue="{Binding CurrentProduct.CategoryId, UpdateSourceTrigger=PropertyChanged}"
                      Height="28"
                      FontSize="14"
                      Padding="3"/>

            <TextBlock Text="Производитель"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,15,0,3"/>
            <ComboBox x:Name="productManufacturerComboBox"
                      ItemsSource="{Binding Manufacturers}"
                      DisplayMemberPath="ManufacturerTitle"
                      SelectedValuePath="ManufacturerId"
                      SelectedValue="{Binding CurrentProduct.ManufacturerId, UpdateSourceTrigger=PropertyChanged}"
                      Height="28"
                      FontSize="14"
                      Padding="3"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.Column="2">
            <TextBlock Text="Цена товара (руб.) *"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,0,0,3"/>
            <TextBox x:Name="productPriceTextBox"
                     Text="{Binding CurrentProduct.ProductPrice, UpdateSourceTrigger=PropertyChanged}"
                     Height="28"
                     FontSize="14"
                     Padding="5"/>

            <TextBlock Text="Скидка на товар (%)"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,15,0,3"/>
            <TextBox x:Name="productDiscountTextBox"
                     Text="{Binding CurrentProduct.ProductDiscount, UpdateSourceTrigger=PropertyChanged}"
                     Height="28"
                     FontSize="14"
                     Padding="5"/>

            <TextBlock Text="Количество на складе *"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,15,0,3"/>
            <TextBox x:Name="productQuantityInStockTextBox"
                     Text="{Binding CurrentProduct.ProductQuantityInStock, UpdateSourceTrigger=PropertyChanged}"
                     Height="28"
                     FontSize="14"
                     Padding="5"/>

            <TextBlock Text="Единица измерения *"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,15,0,3"/>
            <TextBox x:Name="productUnitTextBox"
                     Text="{Binding CurrentProduct.ProductUnit, UpdateSourceTrigger=PropertyChanged}"
                     Height="28"
                     FontSize="14"
                     Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="3">
            <TextBlock Text="Описание товара"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,0,0,3"/>
            <TextBox x:Name="productDescriptionTextBox"
                     Text="{Binding CurrentProduct.ProductDescription, UpdateSourceTrigger=PropertyChanged}"
                     Height="80"
                     FontSize="14"
                     Padding="5"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <StackPanel Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="3">
            <TextBlock Text="Поставщик товара"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,0,0,3"/>
            <ComboBox x:Name="productProviderComboBox"
                      ItemsSource="{Binding Providers}"
                      DisplayMemberPath="ProviderTitle"
                      SelectedValuePath="ProviderId"
                      SelectedValue="{Binding CurrentProduct.ProviderId, UpdateSourceTrigger=PropertyChanged}"
                      Height="28"
                      FontSize="14"
                      Padding="3"/>
        </StackPanel>

        <StackPanel Grid.Row="10" Grid.Column="0" Grid.ColumnSpan="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button x:Name="saveProductButton" 
                    Content="Сохранить"
                    Width="120"
                    Height="35"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Margin="5"
                    Background="#4CAF50"
                    Foreground="White"
                    Click="saveProductButton_Click"/>

            <Button x:Name="cancelButton" 
                    Content="Отмена"
                    Width="120"
                    Height="35"
                    FontSize="14"
                    Margin="5"
                    Click="cancelButton_Click"/>
        </StackPanel>

    </Grid>
</Window>